# xdg-desktop-portal-hyprland.
# https://wiki.hypr.land/Hypr-Ecosystem/xdg-desktop-portal-hyprland/

screencopy {
    # Remember a screen share selection so the picker doesn't reappear every
    # time the same app asks again in a session.
    allow_token_by_default = true
}
